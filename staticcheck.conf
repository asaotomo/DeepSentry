# DeepSentry v2.0.4 staticcheck policy.
checks = ["inherit", "-ST1005"]

# User-facing errors intentionally begin with protocol and product names such
# as FTP, Telnet, Skill and MCP. Lowercasing those names would make the Chinese
# diagnostics less consistent without improving composability.
